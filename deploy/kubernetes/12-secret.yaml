# TEMPLATE — do not commit real secrets. Use sealed-secrets or external-secrets.
apiVersion: v1
kind: Secret
metadata: { name: aegis-secret, namespace: aegis }
type: Opaque
stringData:
  database-url: "postgres://aegis:CHANGE_ME@postgres.aegis.svc:5432/aegis?sslmode=disable"
  jwt-secret: "CHANGE_ME_32_BYTES_MIN"
  s3-access-key: "CHANGE_ME"
  s3-secret-key: "CHANGE_ME"
  bootstrap-admin-email: "admin@aegis.local"
  bootstrap-admin-password: "CHANGE_ME"
