// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: aegis/connector/v1/connector.proto

package connectorv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ConnectorService_Enroll_FullMethodName      = "/aegis.connector.v1.ConnectorService/Enroll"
	ConnectorService_GetConfig_FullMethodName   = "/aegis.connector.v1.ConnectorService/GetConfig"
	ConnectorService_WatchConfig_FullMethodName = "/aegis.connector.v1.ConnectorService/WatchConfig"
	ConnectorService_Heartbeat_FullMethodName   = "/aegis.connector.v1.ConnectorService/Heartbeat"
	ConnectorService_GetSelf_FullMethodName     = "/aegis.connector.v1.ConnectorService/GetSelf"
	ConnectorService_BindDevice_FullMethodName  = "/aegis.connector.v1.ConnectorService/BindDevice"
)

// ConnectorServiceClient is the client API for ConnectorService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ConnectorService is the UNIFIED enrollment and control protocol for every
// externally connected component of the platform: endpoint agents, remote
// scanners, external collectors, and future component kinds.
//
// All kinds share the same connection protocol and differ only in the role
// logic that runs on top of the distributed configuration:
//
//  1. An operator creates the connection in the UI and receives a
//     one-time connect command:
//     aegis-connector --connect <host>:<port>/<enroll_token>
//  2. The component dials gRPC and calls Enroll with that token. If the
//     backend accepts it, the connector record is marked active, the token
//     is burned (further init attempts with the same token are rejected)
//     and the allow message is returned: a long-lived connector secret,
//     the canonical connection endpoint info and the current configuration.
//  3. The component persists the credentials to a local config file
//     (optionally installs itself as a system service) and starts work.
//  4. On every (re)connect the component calls GetConfig to reload its
//     configuration, keeps a WatchConfig stream open for hot reload
//     without restarts, and heartbeats liveness + role status.
//
// Continued-run authentication uses per-call metadata:
//
//	x-aegis-connector-id:     <connector_id>
//	x-aegis-connector-secret: <secret issued at enrollment>
//
// Transport is gRPC; TLS when the platform endpoint terminates TLS,
// plaintext is accepted only for local development.
type ConnectorServiceClient interface {
	// Enroll exchanges the one-time connect token for a long-lived connector
	// secret plus endpoint/config info. The token is single-use: a second
	// call with the same token is rejected.
	Enroll(ctx context.Context, in *EnrollRequest, opts ...grpc.CallOption) (*EnrollResponse, error)
	// GetConfig returns the connector's current configuration. Called by the
	// component on every connection so config changes take effect on
	// reconnect even without a live watch stream.
	GetConfig(ctx context.Context, in *GetConfigRequest, opts ...grpc.CallOption) (*ConnectorConfig, error)
	// WatchConfig streams configuration updates for hot reload. The first
	// update carries the current configuration; every later update is pushed
	// when the configuration is changed from the UI/API.
	WatchConfig(ctx context.Context, in *WatchConfigRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ConfigUpdate], error)
	// Heartbeat reports liveness and role status. The response carries the
	// server's current config version so the component can re-fetch when it
	// missed a watch update (belt-and-braces with WatchConfig).
	Heartbeat(ctx context.Context, in *HeartbeatRequest, opts ...grpc.CallOption) (*HeartbeatResponse, error)
	// GetSelf returns the connector's own record as the server sees it
	// (used by `aegis-connector status`).
	GetSelf(ctx context.Context, in *GetSelfRequest, opts ...grpc.CallOption) (*ConnectorSelf, error)
	// BindDevice attaches (or refreshes) the device identity of an
	// agent-kind connector: the endpoint generates a CSR locally — the
	// private key never leaves — and the server creates or updates the
	// bound device record and returns its certificate. Authenticated with
	// connector credentials; rejected for every other kind. This replaces
	// the old standalone agent enrollment (removed in v1.24.0).
	BindDevice(ctx context.Context, in *BindDeviceRequest, opts ...grpc.CallOption) (*BindDeviceResponse, error)
}

type connectorServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewConnectorServiceClient(cc grpc.ClientConnInterface) ConnectorServiceClient {
	return &connectorServiceClient{cc}
}

func (c *connectorServiceClient) Enroll(ctx context.Context, in *EnrollRequest, opts ...grpc.CallOption) (*EnrollResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(EnrollResponse)
	err := c.cc.Invoke(ctx, ConnectorService_Enroll_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *connectorServiceClient) GetConfig(ctx context.Context, in *GetConfigRequest, opts ...grpc.CallOption) (*ConnectorConfig, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ConnectorConfig)
	err := c.cc.Invoke(ctx, ConnectorService_GetConfig_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *connectorServiceClient) WatchConfig(ctx context.Context, in *WatchConfigRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ConfigUpdate], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &ConnectorService_ServiceDesc.Streams[0], ConnectorService_WatchConfig_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[WatchConfigRequest, ConfigUpdate]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ConnectorService_WatchConfigClient = grpc.ServerStreamingClient[ConfigUpdate]

func (c *connectorServiceClient) Heartbeat(ctx context.Context, in *HeartbeatRequest, opts ...grpc.CallOption) (*HeartbeatResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HeartbeatResponse)
	err := c.cc.Invoke(ctx, ConnectorService_Heartbeat_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *connectorServiceClient) GetSelf(ctx context.Context, in *GetSelfRequest, opts ...grpc.CallOption) (*ConnectorSelf, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ConnectorSelf)
	err := c.cc.Invoke(ctx, ConnectorService_GetSelf_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *connectorServiceClient) BindDevice(ctx context.Context, in *BindDeviceRequest, opts ...grpc.CallOption) (*BindDeviceResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(BindDeviceResponse)
	err := c.cc.Invoke(ctx, ConnectorService_BindDevice_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ConnectorServiceServer is the server API for ConnectorService service.
// All implementations must embed UnimplementedConnectorServiceServer
// for forward compatibility.
//
// ConnectorService is the UNIFIED enrollment and control protocol for every
// externally connected component of the platform: endpoint agents, remote
// scanners, external collectors, and future component kinds.
//
// All kinds share the same connection protocol and differ only in the role
// logic that runs on top of the distributed configuration:
//
//  1. An operator creates the connection in the UI and receives a
//     one-time connect command:
//     aegis-connector --connect <host>:<port>/<enroll_token>
//  2. The component dials gRPC and calls Enroll with that token. If the
//     backend accepts it, the connector record is marked active, the token
//     is burned (further init attempts with the same token are rejected)
//     and the allow message is returned: a long-lived connector secret,
//     the canonical connection endpoint info and the current configuration.
//  3. The component persists the credentials to a local config file
//     (optionally installs itself as a system service) and starts work.
//  4. On every (re)connect the component calls GetConfig to reload its
//     configuration, keeps a WatchConfig stream open for hot reload
//     without restarts, and heartbeats liveness + role status.
//
// Continued-run authentication uses per-call metadata:
//
//	x-aegis-connector-id:     <connector_id>
//	x-aegis-connector-secret: <secret issued at enrollment>
//
// Transport is gRPC; TLS when the platform endpoint terminates TLS,
// plaintext is accepted only for local development.
type ConnectorServiceServer interface {
	// Enroll exchanges the one-time connect token for a long-lived connector
	// secret plus endpoint/config info. The token is single-use: a second
	// call with the same token is rejected.
	Enroll(context.Context, *EnrollRequest) (*EnrollResponse, error)
	// GetConfig returns the connector's current configuration. Called by the
	// component on every connection so config changes take effect on
	// reconnect even without a live watch stream.
	GetConfig(context.Context, *GetConfigRequest) (*ConnectorConfig, error)
	// WatchConfig streams configuration updates for hot reload. The first
	// update carries the current configuration; every later update is pushed
	// when the configuration is changed from the UI/API.
	WatchConfig(*WatchConfigRequest, grpc.ServerStreamingServer[ConfigUpdate]) error
	// Heartbeat reports liveness and role status. The response carries the
	// server's current config version so the component can re-fetch when it
	// missed a watch update (belt-and-braces with WatchConfig).
	Heartbeat(context.Context, *HeartbeatRequest) (*HeartbeatResponse, error)
	// GetSelf returns the connector's own record as the server sees it
	// (used by `aegis-connector status`).
	GetSelf(context.Context, *GetSelfRequest) (*ConnectorSelf, error)
	// BindDevice attaches (or refreshes) the device identity of an
	// agent-kind connector: the endpoint generates a CSR locally — the
	// private key never leaves — and the server creates or updates the
	// bound device record and returns its certificate. Authenticated with
	// connector credentials; rejected for every other kind. This replaces
	// the old standalone agent enrollment (removed in v1.24.0).
	BindDevice(context.Context, *BindDeviceRequest) (*BindDeviceResponse, error)
	mustEmbedUnimplementedConnectorServiceServer()
}

// UnimplementedConnectorServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedConnectorServiceServer struct{}

func (UnimplementedConnectorServiceServer) Enroll(context.Context, *EnrollRequest) (*EnrollResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Enroll not implemented")
}
func (UnimplementedConnectorServiceServer) GetConfig(context.Context, *GetConfigRequest) (*ConnectorConfig, error) {
	return nil, status.Error(codes.Unimplemented, "method GetConfig not implemented")
}
func (UnimplementedConnectorServiceServer) WatchConfig(*WatchConfigRequest, grpc.ServerStreamingServer[ConfigUpdate]) error {
	return status.Error(codes.Unimplemented, "method WatchConfig not implemented")
}
func (UnimplementedConnectorServiceServer) Heartbeat(context.Context, *HeartbeatRequest) (*HeartbeatResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Heartbeat not implemented")
}
func (UnimplementedConnectorServiceServer) GetSelf(context.Context, *GetSelfRequest) (*ConnectorSelf, error) {
	return nil, status.Error(codes.Unimplemented, "method GetSelf not implemented")
}
func (UnimplementedConnectorServiceServer) BindDevice(context.Context, *BindDeviceRequest) (*BindDeviceResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method BindDevice not implemented")
}
func (UnimplementedConnectorServiceServer) mustEmbedUnimplementedConnectorServiceServer() {}
func (UnimplementedConnectorServiceServer) testEmbeddedByValue()                          {}

// UnsafeConnectorServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ConnectorServiceServer will
// result in compilation errors.
type UnsafeConnectorServiceServer interface {
	mustEmbedUnimplementedConnectorServiceServer()
}

func RegisterConnectorServiceServer(s grpc.ServiceRegistrar, srv ConnectorServiceServer) {
	// If the following call panics, it indicates UnimplementedConnectorServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ConnectorService_ServiceDesc, srv)
}

func _ConnectorService_Enroll_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(EnrollRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorServiceServer).Enroll(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorService_Enroll_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorServiceServer).Enroll(ctx, req.(*EnrollRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ConnectorService_GetConfig_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetConfigRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorServiceServer).GetConfig(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorService_GetConfig_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorServiceServer).GetConfig(ctx, req.(*GetConfigRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ConnectorService_WatchConfig_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(WatchConfigRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(ConnectorServiceServer).WatchConfig(m, &grpc.GenericServerStream[WatchConfigRequest, ConfigUpdate]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ConnectorService_WatchConfigServer = grpc.ServerStreamingServer[ConfigUpdate]

func _ConnectorService_Heartbeat_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HeartbeatRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorServiceServer).Heartbeat(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorService_Heartbeat_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorServiceServer).Heartbeat(ctx, req.(*HeartbeatRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ConnectorService_GetSelf_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetSelfRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorServiceServer).GetSelf(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorService_GetSelf_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorServiceServer).GetSelf(ctx, req.(*GetSelfRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ConnectorService_BindDevice_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(BindDeviceRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorServiceServer).BindDevice(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorService_BindDevice_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorServiceServer).BindDevice(ctx, req.(*BindDeviceRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// ConnectorService_ServiceDesc is the grpc.ServiceDesc for ConnectorService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ConnectorService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "aegis.connector.v1.ConnectorService",
	HandlerType: (*ConnectorServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Enroll",
			Handler:    _ConnectorService_Enroll_Handler,
		},
		{
			MethodName: "GetConfig",
			Handler:    _ConnectorService_GetConfig_Handler,
		},
		{
			MethodName: "Heartbeat",
			Handler:    _ConnectorService_Heartbeat_Handler,
		},
		{
			MethodName: "GetSelf",
			Handler:    _ConnectorService_GetSelf_Handler,
		},
		{
			MethodName: "BindDevice",
			Handler:    _ConnectorService_BindDevice_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "WatchConfig",
			Handler:       _ConnectorService_WatchConfig_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "aegis/connector/v1/connector.proto",
}
